import Success from "@/components/common-layout/Payment/Success";
import React, { Suspense } from "react";

const SuccessPage = () => {
  return (
    <Suspense>
      <Success />
    </Suspense>
  );
};

export default SuccessPage;
