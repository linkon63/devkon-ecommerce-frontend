export enum tagTypes {
  add_to_cart = "add_to_cart",
  send_otp = "send_otp",
  profile = "profile",
  country = "country",
  order = "order",
  hero_section = "hero_section",
}

export const tagTypesList = [
  tagTypes?.add_to_cart,
  tagTypes?.send_otp,
  tagTypes?.profile,
  tagTypes?.country,
  tagTypes?.order,
  tagTypes?.hero_section
];
